# Puerto del backend
PORT=3000

# Orígenes permitidos (separados por comas, sin espacios)
FRONTEND_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# true si vas a usar cookies/sesión entre front y back
CORS_CREDENTIALS=true

# (Opcional) Si más adelante usas Supabase desde el backend, añade aquí:
# SUPABASE_URL=https://xxxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJI...   # ⚠️ NO lo expongas en el frontend
